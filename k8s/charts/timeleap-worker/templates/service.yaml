apiVersion: v1
kind: Service
metadata:
  name: {{ include "timeleap-worker.fullname" . }}
  labels:
    {{- include "timeleap-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "timeleap-worker.selectorLabels" . | nindent 4 }}
