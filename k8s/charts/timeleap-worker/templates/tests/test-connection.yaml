apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "timeleap-worker.fullname" . }}-test-connection"
  labels:
    {{- include "timeleap-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "timeleap-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
